{% extends 'base.html.twig' %}

{% block title %}Détail d'un événement
{% endblock %}

{% block body %}
    <div class="container mt-4">
        <div class="row">
            <div class="col-md-7"></divclass="col-md-5">
            <h1 class="text-center">{{ myEvent.getTitre}}</h1>
            <h6 class="card-title text-center">Du
                <b>
                    {{ myEvent.dateDebutEvent|localizeddate('long', 'short') }}</b>
                au
                <b>
                    {{ myEvent.dateFinEvent|localizeddate('long', 'short') }}</b>
            </h6>
            <h3>{{ myEvent.corps }}</h3>
        </div>
    </div>

{% endblock %}
